# DR-2 checkpoint 2 deferment — 2026-09-23

Status: **SAFE-PAUSED / OPERATOR DEFERMENT / NO EXTERNAL TARGET SELECTED**

Issue: #277

## Operator decision

The operator explicitly chose to postpone DR-2 physical-independence target selection.

Interim backup posture:

- local backup is acceptable for the current operating phase;
- an encrypted Google Drive copy may be used later as a secondary off-device copy;
- Google Drive is not yet selected, integrated, or validated as a DR-2 recovery target;
- no external VPS/object-store target is selected;
- no paid infrastructure is authorized;
- no checkpoint 3 host-evidence/preflight starts from this decision.

## Claim boundary

This deferment does not alter the already-closed original Off-host DR result: total loss of the tested SumoPod staging host remains proven recoverable at the documented Issue #266 boundary.

It also does not prove DR-2 physical independence. Local backup can share a physical failure domain with replacement compute, and a future Google Drive copy would count only as an additional off-device backup until its transport, retention, retrieval, and clean-host recovery path are explicitly tested.

## Google Drive safety boundary

If Google Drive is used later, upload only encrypted DR artifacts generated by the reviewed backup tooling plus non-secret manifests/checksum metadata as appropriate.

Do not upload:

- DR private decryption keys;
- SSH private keys;
- raw deployment secrets;
- operator credentials;
- Vault master keys;
- unencrypted volume/database contents.

The private recovery key and runtime secrets must remain separately protected so loss or compromise of the Drive account alone cannot expose the recovered application data.

## Safe resume point

When the operator wants to resume DR-2:

1. decide whether to use an external Linux/SSH target or implement/review a Google Drive transport;
2. explicitly select the target;
3. close checkpoint 2 custody/trust for that target;
4. only then begin checkpoint 3 physical-independence host evidence/preflight.

Until then, DR-2 checkpoint 2 remains **DEFERRED / NOT CLOSED**.
